# 回滚记录

## 回滚点 R0

### 时间
2026-04-18

### 名称
基础项目框架初始化

### 回滚点说明
这是项目的第一个结构化回滚点。此时仓库只完成了目录骨架、占位脚本、基础配置和记录系统搭建，还没有进入真实模型和数据实现阶段。

### 对应状态
- 已有 `README.md`、`pyproject.toml`、`.gitignore`
- 已有 `configs/`、`data/`、`outputs/`、`scripts/`、`src/`、`tests/`
- 已有最小可导入的 Python 包结构
- 已有 `memory` 下的项目记录文件

### 如果后续需要回到这个状态
应保留：
- 项目目录骨架
- 说明文档
- 记录文档

应移除：
- 后续新增的训练实现
- 后续新增的数据处理逻辑
- 后续新增的实验配置
- 后续新增的模型适配代码

### 备注
当前仓库尚未初始化版本控制，因此这里的“回滚”首先作为人工结构回滚记录使用。后续如果初始化 `git`，建议让每个回滚点同步对应一个提交或标签。
