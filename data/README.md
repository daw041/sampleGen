# Data Workspace

This directory is intentionally split to keep the ML workflow clean.

- `raw/`: original downloaded or collected audio
- `interim/`: temporary outputs during cleaning and analysis
- `processed/`: training-ready audio
- `metadata/`: CSV/JSON metadata files for `v1` and `v2`

Do not commit large datasets into the repository.
